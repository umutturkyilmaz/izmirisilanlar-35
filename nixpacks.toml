[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

[variables]
NIXPACKS_NODE_VERSION = "22.12.0"
NODE_ENV = "production"
